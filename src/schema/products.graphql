extend type Query {
  product(id: ID!): Product
  products(slug: String): [Product]
}
 
type Product {
  id: ID!
  name: String!
  slug: String!
  description: String!
  price: Int!
  image: String!
}
